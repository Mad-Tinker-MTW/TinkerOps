node_modules/
dist/
.env
.env.local
.DS_Store

# Python worker cache
__pycache__/
*.pyc

# Registry backup snapshots (kept on disk, not versioned)
Data/backups/

# MCP configs and API keys (Brave key lives in ~/.lmstudio/mcp.json — never commit a copy)
mcp.json
*.mcp.json
.lmstudio/
*.key
config.toml
